(function(){const APPS_SCRIPT_URL="";const forms=document.querySelectorAll('.capture-form');forms.forEach(form=>{form.addEventListener('submit',async function(e){e.preventDefault();if(!form.reportValidity())return;const button=form.querySelector('button[type="submit"]');const feedback=form.querySelector('.form-feedback');const service=form.dataset.service;const destination=form.dataset.destination||'';const payload={nome:form.nome.value.trim(),whatsapp:form.whatsapp.value.trim(),email:form.email.value.trim(),origem:form.origem.value,servico:service,pagina:location.href,dataHora:new Date().toISOString()};button.disabled=true;feedback.textContent='Registrando seus dados...';try{if(APPS_SCRIPT_URL){await fetch(APPS_SCRIPT_URL,{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify(payload)});}if(service==='psicoterapia'){const offer=document.getElementById('oferta-psicoterapia');offer.hidden=false;offer.scrollIntoView({behavior:'smooth',block:'start'});feedback.textContent='Cadastro concluído. Confira abaixo o valor e as condições.';}else if(destination){feedback.textContent='Cadastro concluído. Abrindo a oferta...';setTimeout(()=>{location.href=destination},450);}}catch(err){feedback.textContent='Não foi possível concluir o cadastro. Tente novamente.';}finally{button.disabled=false;}});});})();